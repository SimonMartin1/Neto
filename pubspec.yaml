name: neto
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.0

dependencies:
  app_links: ^7.0.0
  dio: ^5.9.1
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  mobile_scanner: ^7.1.4
  permission_handler: ^12.0.1
  qr_flutter: ^4.1.0
  share_plus: ^12.0.1
  shared_preferences: ^2.5.4
  uuid: ^4.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo_neto.png
